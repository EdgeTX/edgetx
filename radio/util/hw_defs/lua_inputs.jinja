//
// WARNING: DO NOT EDIT THIS FILE
//   This file has been generated from the target's JSON hardware description
//

{% macro lua_input(src_type, offset, input) %}
  {% set li = legacy_inputs[input.name] %}
{ SourceRef_({{ src_type }}, {{ offset }}).toUint32(), "{{ li.lua }}", "{{ li.description }}" },
{%- endmacro %}

{% macro lua_inputs(src_type, inputs) %}
  {% for input in inputs %}
  {{ lua_input(src_type, loop.index0, input) }}
  {% endfor %}
{%- endmacro %}

static const LuaSingleField _lua_inputs[] = {
  // main inputs
  {% set main_inputs = adc_inputs.inputs | selectattr('type', '==', 'STICK') | list %}
{{ lua_inputs('SOURCE_TYPE_STICK', main_inputs) }}
  // flex inputs
  {% set inputs = adc_inputs.inputs | selectattr('type', '==', 'FLEX') | list %}
{{ lua_inputs('SOURCE_TYPE_POT', inputs) }}
  // switches
  {% set inputs = switches | selectattr('type','!=','FSWITCH') | list %}
  {% set sw_count =  inputs | count %}
  {% for n in range(sw_count) %}
    {% set sw = switches[loop.index0] %}
  { SourceRef_(SOURCE_TYPE_SWITCH, {{ loop.index0 }}).toUint32(), "{{ sw.name }}", "{{ sw.name }}" },
  {% endfor %}
  // trims
  {% set trims_count =  trims | count %}
  {% for n in range(trims_count) %}
    {% if n < (main_inputs | length) %}
      {% set input = main_inputs[loop.index0] %}
      {% set li = legacy_inputs[input.name] %}
  { SourceRef_(SOURCE_TYPE_TRIM, {{ loop.index0 }}).toUint32(), "trim-{{ li.lua }}", "{{ li.description}} trim" },
    {% else %}
  { SourceRef_(SOURCE_TYPE_TRIM, {{ loop.index0 }}).toUint32(), "trim-t{{ loop.index }}", "Aux trim T{{ loop.index }}" },
    {% endif %}
  {% endfor %}
};
