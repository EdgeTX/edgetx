# Simulator UI fixtures

The minimal TX16S settings template is derived from the fixture contributed by
Mateusz Urban (`onliner10`) in [EdgeTX pull request #7337](https://github.com/EdgeTX/edgetx/pull/7337).
It remains an immutable template: every flow copies `settings/` and `sdcard/`
into a unique run directory before launching the simulator.
